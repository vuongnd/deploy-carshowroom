

<%= form_for([@event, @event.comments.new]) do |f| %>
<div class="field">
<%= f.label :name %><br />
<%= f.text_field :name %>
</div>
<div class="field">
<%= f.label :body %><br />
<%= f.text_area :body, :class=> "text_area" %>
</div>
<div class="actions">
<%= f.submit 'Add' %>
</div>
<% end %>