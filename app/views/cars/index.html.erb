<!-- 
<table>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.name %></td>
        <td><%= image_tag car.image.url(:medium) %></td>
        <td><%= car.year %></td>
        <td><%= car.madein %></td>
        <td><%= car.color %></td>
        <td><%= car.max_speed %></td>
        <td><%= car.fuel_consumption %></td>
        <td><%= car.engine_capacity %></td>
        <td><%= car.acceleration %></td>
        <td><%= car.wheel_size %></td>
        <td><%= car.braking_system %></td>
        <td><%= car.safety_system %></td>
        <td><%= image_tag car.typeofcar.company.logo.url(:thumb) %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Car', new_car_path %>
 -->
  <div class="menutop" id="top">
    <nav id="" class=" nav nav-bar menu-home-page">
      <ul>
        <li><a href="<%= homepage_path %>" title="" class="hvr-shutter-in-vertical">Home</a></li>
        <li><a href="<%= typeofcar_path(1) %>" title="">View by Categories</a></li>
        <li><a href="<%= events_path %>" title="">Events</a></li>
        <li><a href="<%= faq_path %>" title="">FAQ</a></li>
        <li><a href="<%= new_contact_path %>" title="">Contacts</a></li>
        
      </ul>
    </nav>
    <a class="back" href="<%= url_for(:back) %>" title="">Back</a>
  </div>
  <div class="notify">
    <marquee><%= @eventlast.title %></marquee>
  </div>

  <div class="">
  <div id="index">
    <% @cars.each do |car| %>
      <div class="col-sm-6 col-md-4 index-products">
        <div class="thumbnail products">
          <div class="image-index image-index-cars">
            <%= link_to (image_tag car.image.url(:medium), lazy: true ),car %>
            <%= image_tag car.typeofcar.company.logo.url(), lazy: true  %>
          </div>
          <div class="caption">
            <h3><a href="viewcar.html" title=""><%= link_to car.name, car %></a></h3>
            <p><%= car.prices %> $</p>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="footer">
    <!-- <div class="paginations">
      <ul class="pagination">
          <li><a href="#"><i class="glyphicon glyphicon-fast-backward"></i></a></li>
          <li><a href="#">1</a></li>
          <li><a href="#">2</a></li>
          <li><a href="#">3</a></li>
          <li><a href="#">4</a></li>
          <li><a href="#">...</a></li>
          <li><a href="#"><i class="glyphicon glyphicon-fast-forward"></i></a></li>
        </ul>
    </div> -->
    <div class="logocompanyindex">
      <a>Thanh Cong Group</a>
    </div>
    <div class="backtotop">
      <a href="#top" title=""><img src="system/localimage/logo/backtotop.png" alt=""></a>
    </div>
  </div>
  
  </div>